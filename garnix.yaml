builds:
 include:
   - darwinConfigurations.*
   - nixosConfigurations.*
   - packages.*.*
   - devShells.*.*
 exclude:
   - darwinConfigurations.macbook # sandbox
   - nixosConfigurations.lagern   # impure
   - nixosConfigurations.odake    # impure
   - nixosConfigurations.walberla # impure
   - packages.*.pcaddy            # impure
   - packages.*.spotify           # unfree
